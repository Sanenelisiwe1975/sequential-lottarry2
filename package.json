{
  "name": "sequential-lottery",
  "version": "1.0.0",
  "description": "Full-stack decentralized lottery DApp with backend API",
  "scripts": {
    "install:all": "cd smart-contracts && npm install && cd ../frontend && npm install && cd ../backend && npm install",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:contracts": "cd smart-contracts && npm run compile",
    "build:all": "npm run build:contracts && npm run build:backend && npm run build:frontend"
  },
  "keywords": [
    "lottery",
    "blockchain",
    "dapp",
    "chainlink",
    "vrf",
    "ethereum",
    "nextjs",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
